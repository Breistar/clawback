# Copy to .env — never commit the real values
#
# Local dev (npm run dev):     API_PORT=3002  (3001 if clawback-prototype is NOT running)
# Docker / Vultr (port 80):    docker-compose sets API_PORT=80 automatically

VULTR_INFERENCE_API_KEY=your-key-here
API_PORT=3002
